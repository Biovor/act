{% extends 'base.html.twig' %}

{% block title %}AlimentationsType index{% endblock %}

{% block body %}
    <h1>AlimentationsType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for alimentations_type in alimentations_types %}
            <tr>
                <td>{{ alimentations_type.id }}</td>
                <td>{{ alimentations_type.nom }}</td>
                <td>
                    <a href="{{ path('alimentations_type_show', {'id': alimentations_type.id}) }}">show</a>
                    <a href="{{ path('alimentations_type_edit', {'id': alimentations_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('alimentations_type_new') }}">Create new</a>
{% endblock %}
