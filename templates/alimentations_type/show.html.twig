{% extends 'base.html.twig' %}

{% block title %}AlimentationsType{% endblock %}

{% block body %}
    <h1>AlimentationsType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alimentations_type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ alimentations_type.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('alimentations_type_index') }}">back to list</a>

    <a href="{{ path('alimentations_type_edit', {'id': alimentations_type.id}) }}">edit</a>

    {{ include('alimentations_type/_delete_form.html.twig') }}
{% endblock %}
